# ┌─────────────────────────────────┐
# │ 部署模式选择（二选一）            │
# └─────────────────────────────────┘
# 模式1：本地IP访问（无域名）【默认】
# LOCAL_PROTOCOL=http
# LOCAL_IP=10.10.10.10

# 模式2：自定义域名访问（启用时取消下方注释）
LOBE_DOMAIN=https://lobe.example.com
CASDOOR_DOMAIN=https://casdoor.example.com
MINIO_API_DOMAIN=https://lobe-s3-api.example.com
MINIO_CONSOLE_DOMAIN=https://lobe-s3-ui.example.com

# ┌─────────────────────────────────┐
# │ 端口配置（所有模式通用）          │
# └─────────────────────────────────┘
PG_PORT=3999                    # PostgreSQL 数据库端口
LOBE_PORT=4000                  # LobeChat 服务端口
CASDOOR_PORT=4002               # Casdoor 认证服务端口
MINIO_API_PORT=9000             # MinIO API 端口
MINIO_CONSOLE_PORT=9001         # MinIO 控制台端口

# ┌─────────────────────────────────┐
# │ 数据持久化公共路径配置            │
# └─────────────────────────────────┘
VOLUME_BASE=/opt/data   # 📁 所有服务数据持久化的公共目录(不影响 compose)

PG_VOLUME=$VOLUME_BASE/pg
MINIO_VOLUME=$VOLUME_BASE/minio
LOBE_VOLUME=$VOLUME_BASE/lobe

# ┌─────────────────────────────────┐
# │ PostgreSQL 数据库配置           │
# └─────────────────────────────────┘
POSTGRES_USER=lobe              # 数据库用户名
POSTGRES_PASSWORD=              # ✅ 修改为强密码
POSTGRES_DB=lobechat            # LobeChat 数据库名称
CASDOOR_DB_NAME=casdoor         # Casdoor 数据库名称

# ┌─────────────────────────────────┐
# │ MinIO 对象存储配置              │
# └─────────────────────────────────┘
MINIO_ROOT_USER=minio           # MinIO 管理员用户名
MINIO_ROOT_PASSWORD=            # MinIO 管理员密码
MINIO_LOBE_BUCKET=lobe          # MinIO 存储桶名称

# ┌─────────────────────────────────┐
# │ 安全密钥（必填！）               │
# └─────────────────────────────────┘
# ⚠️ 首次启动前请运行以下命令生成（Linux/macOS）：
#   openssl rand -base64 32
# 然后将输出填入下方：
NEXT_AUTH_SECRET=
KEY_VAULTS_SECRET=

# ┌─────────────────────────────────┐
# │ 服务集成密钥（启动后获取）       │
# └─────────────────────────────────┘
# 🔑 MinIO 密钥（启动后从 MinIO 控制台获取）
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# 🆔 Casdoor 应用密钥（启动后从 Casdoor 控制台获取）
AUTH_CASDOOR_ID=
AUTH_CASDOOR_SECRET=